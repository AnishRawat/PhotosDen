{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "package": "rm -f function.zip && npm ci && npm run build && rm -rf node_modules && npm ci --omit=dev && cd dist && zip -r ../function.zip . && cd .. && zip -r function.zip node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/crypto-js": "^4.2.2",
    "@types/node": "^25.0.10",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.975.0",
    "@aws-sdk/client-dynamodb": "^3.975.0",
    "@aws-sdk/client-s3": "^3.983.0",
    "@aws-sdk/client-ssm": "^3.975.0",
    "@aws-sdk/credential-providers": "^3.975.0",
    "@aws-sdk/lib-dynamodb": "^3.975.0",
    "@aws-sdk/s3-request-presigner": "^3.983.0",
    "@aws-sdk/util-dynamodb": "^3.975.0",
    "aws-jwt-verify": "^5.1.1",
    "zod": "^4.3.6"
  }
}
