name: photosden_frontend
description: A new Flutter project for PhotosDen.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  go_router: ^17.1.0
  dio: ^5.9.1
  cryptography: ^2.9.0
  image: ^4.7.2
  http: ^1.6.0
  image_picker: ^1.2.1
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
